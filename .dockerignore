node_modules
**/node_modules
**/.next
**/dist
**/.turbo
**/coverage
**/*.tsbuildinfo

.git
.github
# Anchored recursively on purpose: a bare `.env.*` matches only the context
# root, so `apps/web/.env.local` would be copied into the build layer — and into
# the remote builder's cache, where rotating the values is the only remedy.
**/.env
**/.env.*
!.env.example

# Reference material and planning records — not part of the image.
design
docs
plans
supabase/.branches
supabase/.temp
*.pdf
*.md
!README.md
