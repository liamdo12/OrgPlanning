/**
 * Glass design system barrel.
 *
 * Tokens, primitives (GlassCard, Button, Field, Badge, DataTable) and the admin
 * patterns are exported from here. This package may never import
 * `@occasion/core` or `@occasion/db` — domain data arrives as props.
 */
export {};
